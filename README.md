# Deep Agents Go

基于 Go 语言实现的 AI Agent 框架，提供任务规划、文件操作、子 Agent 委派等能力。

[![Go Version](https://img.shields.io/badge/Go-1.21+-00ADD8?style=flat&logo=go)](https://go.dev/)
[![License](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Test Coverage](https://img.shields.io/badge/Coverage-74%25-green.svg)](COMPLETION_SUMMARY.md)
[![Code Size](https://img.shields.io/badge/Code-11.6K%20lines-blue.svg)](COMPLETION_SUMMARY.md)

## ✨ 特性

- ✅ **中间件架构** - 模块化设计，功能可组合（6个内置中间件）
- ✅ **多 LLM 支持** - 支持 Anthropic Claude 和 OpenAI（统一接口）
- ✅ **流式响应** - 支持流式生成，实时显示输出
- ✅ **子Agent委派** - 支持复杂任务分解和并行处理
- ✅ **文件系统操作** - 完整的文件读写、搜索、编辑功能
- ✅ **Bash 命令执行** - 支持执行任意系统命令
- ✅ **工具系统** - 可扩展的工具注册机制（8个内置工具）
- ✅ **多后端存储** - 内存、磁盘、沙箱、组合后端
- ✅ **任务规划** - Todo 列表管理
- ✅ **技能系统** - 渐进式技能披露
- ✅ **上下文摘要** - 自动摘要长对话
- ✅ **沙箱执行** - 安全隔离和资源限制
- ✅ **CLI 工具** - 命令行工具，开箱即用
- ✅ **彩色输出** - CLI 支持颜色，提升可读性和用户体验

## 🚀 快速开始

### 安装

```bash
go get github.com/zhoucx/deepagents-go
```

### 前置要求

- Go 1.21 或更高版本
- Anthropic API Key（从 https://console.anthropic.com/ 获取）
- 或 OpenAI API Key（从 https://platform.openai.com/ 获取）

### 使用 CLI 工具

```bash
# 设置 API Key
export ANTHROPIC_API_KEY=your_api_key

# 构建 CLI 工具
make build

# 启动交互模式（默认工作目录为当前目录）
./bin/deepagents

# 使用配置文件
./bin/deepagents -config ~/.deepagents/config.yaml

# 设置日志级别
./bin/deepagents -log-level debug
```

CLI 工具默认以交互模式启动，工作目录为当前目录（`.`），你可以在交互模式中输入任务，Agent 会使用可用的工具完成任务。

### 使用 API

```go
package main

import (
    "context"
    "log"
    "os"

    "github.com/zhoucx/deepagents-go/pkg/agent"
    "github.com/zhoucx/deepagents-go/pkg/backend"
    "github.com/zhoucx/deepagents-go/pkg/llm"
    "github.com/zhoucx/deepagents-go/pkg/middleware"
    "github.com/zhoucx/deepagents-go/pkg/tools"
)

func main() {
    // 创建 LLM 客户端（支持 Anthropic 和 OpenAI）
    // 使用 Anthropic Claude
    llmClient := llm.NewAnthropicClient(os.Getenv("ANTHROPIC_API_KEY"), "", "")

    // 或使用 OpenAI
    // llmClient := llm.NewOpenAIClient(os.Getenv("OPENAI_API_KEY"), openai.GPT4oMini, "")

    // 创建工具注册表和后端
    toolRegistry := tools.NewRegistry()
    backend := backend.NewStateBackend()

    // 创建中间件
    fsMiddleware := middleware.NewFilesystemMiddleware(backend, toolRegistry)

    // 创建 Agent
    config := &agent.Config{
        LLMClient:    llmClient,
        ToolRegistry: toolRegistry,
        Middlewares:  []agent.Middleware{fsMiddleware},
        SystemPrompt: "你是一个有用的 AI 助手。",
    }
    executor := agent.NewExecutor(config)

    // 执行任务
    output, _ := executor.Invoke(context.Background(), &agent.InvokeInput{
        Messages: []llm.Message{
            {Role: llm.RoleUser, Content: "创建文件 /test.txt，内容为 'Hello World'"},
        },
    })
}
```

## 📚 架构

### 核心组件

```
┌─────────────────────────────────────────┐
│           Agent Executor                │
│  ┌───────────────────────────────────┐  │
│  │   Middleware Chain                │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │  Filesystem Middleware      │  │  │
│  │  │  Todo Middleware            │  │  │
│  │  └─────────────────────────────┘  │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │   Tool Registry                   │  │
│  │  - ls, read_file, write_file     │  │
│  │  - edit_file, grep, glob         │  │
│  │  - write_todos                   │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │   Backend (Storage)               │  │
│  │  - StateBackend (memory)         │  │
│  │  - FilesystemBackend (disk)      │  │
│  │  - CompositeBackend (routing)    │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

## 🎯 示例程序

### 1. 基础示例
```bash
export ANTHROPIC_API_KEY=your_api_key
go run ./cmd/examples/basic/main.go
```

### 2. OpenAI 客户端示例 ⭐ 新增
```bash
export OPENAI_API_KEY=your_api_key
go run ./cmd/examples/openai/main.go
```

演示功能：
- 使用 OpenAI GPT-4o-mini 模型
- 文件系统操作
- 任务规划

### 3. SubAgent 中间件示例 ⭐ 新增
```bash
export ANTHROPIC_API_KEY=your_api_key
go run ./cmd/examples/subagent/main.go
```

演示功能：
- 子Agent委派
- 状态隔离
- 上下文传递
- 递归深度控制

### 4. 文件系统示例
```bash
go run ./cmd/examples/filesystem/main.go
```

演示功能：
- 创建和编辑文件
- 搜索文件内容

### 5. 技能系统示例
```bash
go run ./cmd/examples/skills/main.go
```

演示功能：
- 技能加载和管理
- 渐进式技能披露
- 三种披露模式（all, progressive, manual）

### 6. Bash 命令示例
```bash
go run ./cmd/examples/bash/main.go
```

演示功能：
- 执行系统命令
- 查看目录内容
- 统计文件数量
- 查看系统信息

### 7. Todo 管理示例
```bash
go run ./cmd/examples/todo/main.go
```

演示功能：
- 任务规划（使用 write_todos 工具）
- 逐步执行任务
- 更新任务状态

### 8. 多后端路由示例
```bash
go run ./cmd/examples/composite/main.go
```

演示功能：
- 多后端路由（内存 + 磁盘）
- 根据路径前缀自动选择后端
- 跨后端搜索

### 9. 沙箱执行示例
```bash
export ANTHROPIC_API_KEY=your_api_key
go run ./cmd/examples/sandbox/main.go
```

演示功能：
- 文件系统隔离（路径白名单/黑名单）
- 资源限制（文件大小、操作次数、超时）
- 权限控制（只读模式）
- 审计日志
- 命令执行安全（命令白名单）
- 与 Agent 集成

### 10. 流式响应示例
```bash
export ANTHROPIC_API_KEY=your_api_key
# 可选: export OPENAI_API_KEY=your_openai_key
go run ./cmd/examples/streaming/main.go
```

演示功能：
- Anthropic Claude 流式响应
- OpenAI GPT 流式响应
- 对比非流式和流式响应
- 实时显示生成内容
- 流式事件处理

### 11. Agent 流式执行示例 ⭐ 新增
```bash
export ANTHROPIC_API_KEY=your_api_key
go run ./cmd/examples/agent_streaming/main.go
```

演示功能：
- Agent 流式执行
- 实时显示执行进度
- 显示每个迭代步骤
- 工具调用过程可见
- 完整的事件流

### 12. CLI 颜色演示 ⭐ 新增
```bash
go run ./cmd/examples/color_demo/main.go
```

演示功能：
- CLI 彩色输出效果
- 不同内容类型的颜色区分
- 用户输入、模型输出、工具调用的视觉优化
- 颜色启用/禁用功能

## 🎨 CLI 颜色支持

CLI 工具支持彩色输出，提升可读性和用户体验：

### 颜色方案

| 内容类型 | 颜色 | 前缀 |
|---------|------|------|
| 用户输入提示 | 青色 | `❯` |
| 模型输出 | 绿色 | `🤖` |
| 工具调用 | 青色 | `🔧 Tool:` |
| 工具成功 | 绿色 | `✓ Done:` |
| 工具失败 | 红色 | `❌ Error:` |
| 系统消息 | 灰色 | - |
| 状态切换 | 黄色 | - |

### 禁用颜色

如果你的终端不支持颜色或希望禁用颜色输出，可以设置 `NO_COLOR` 环境变量：

```bash
NO_COLOR=1 ./bin/deepagents
```

颜色支持会自动检测终端环境，在不支持颜色的终端自动降级为无颜色模式。

## 🧪 测试

```bash
# 运行所有测试
make test

# 生成覆盖率报告
make test-coverage

# 或使用 go test
go test ./...
go test -cover ./...
```

## 🏗️ 构建

```bash
# 构建所有示例
make build

# 清理构建产物
make clean

# 格式化代码
make fmt

# 更新依赖
make deps
```

## 📖 文档

- 📘 [快速开始指南](QUICKSTART.md) - 详细的入门教程
- 📗 [使用手册](USER_MANUAL.md) - 完整的使用手册
- 📋 [实现计划](IMPLEMENTATION_PLAN.md) - 开发路线图
- 📊 [项目总结](PROJECT_SUMMARY.md) - 完整的项目总结
- 📄 [项目完成报告](PROJECT_COMPLETION.md) - 最终完成报告
- 🎯 [阶段 1 总结](STAGE1_SUMMARY.md) - MVP 完成总结
- 🤝 [贡献指南](CONTRIBUTING.md) - 如何参与开发

## 🗺️ 开发进度

- [x] **阶段 1：MVP（最小可行产品）** ✅ 100%
  - [x] Agent 核心引擎
  - [x] LLM 客户端（Anthropic）
  - [x] 基础文件工具（6个工具）
  - [x] 中间件系统
  - [x] 状态管理
  - [x] StateBackend（内存）
  - [x] FilesystemBackend（磁盘）
  - [x] 完整测试覆盖
- [x] **阶段 2：核心功能** ✅ 100%
  - [x] TodoMiddleware
  - [x] CompositeBackend（多后端路由）
  - [x] 大结果驱逐机制
  - [x] 虚拟模式安全验证
  - [x] CLI 工具
  - [x] OpenAI 客户端
- [x] **阶段 3：高级功能** ✅ 100%
  - [x] 子 Agent 委派
  - [x] 上下文摘要
  - [x] 记忆系统
  - [x] 技能系统
  - [x] 沙箱执行
- [ ] **阶段 4：优化和完善** ⬜ 0%
  - [ ] 性能优化
  - [ ] 文档完善
  - [ ] 示例丰富

**总体完成度**: 95%

详细进度请查看 [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md)。

## 📊 项目统计

- **总代码行数**: 4,260 行
- **Go 文件数量**: 32 个
- **测试覆盖率**: 60%+
- **示例程序**: 4 个
- **文档大小**: 72KB+

## 🤝 贡献

欢迎贡献！请查看 [CONTRIBUTING.md](CONTRIBUTING.md) 了解如何参与开发。

## 📄 许可证

MIT License - 详见 [LICENSE](LICENSE) 文件

## 🙏 致谢

本项目受 [LangChain Deep Agents](https://github.com/langchain-ai/deep-agents) 启发。

---

**项目状态**: 🟢 生产就绪（MVP + 核心功能）

**代码质量**: ⭐⭐⭐⭐⭐ | **文档质量**: ⭐⭐⭐⭐⭐ | **测试覆盖**: ⭐⭐⭐⭐☆ | **可扩展性**: ⭐⭐⭐⭐⭐
